<%= form_with(model: public_post) do |form| %>
  <% if public_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(public_post.errors.count, "error") %> prohibited this public_post from being saved:</h2>

      <ul>
        <% public_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
