<%= form_with(model: public_post_comment) do |form| %>
  <% if public_post_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(public_post_comment.errors.count, "error") %> prohibited this public_post_comment from being saved:</h2>

      <ul>
        <% public_post_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
