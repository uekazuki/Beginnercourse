<%= form_with(model: public_customer) do |form| %>
  <% if public_customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(public_customer.errors.count, "error") %> prohibited this public_customer from being saved:</h2>

      <ul>
        <% public_customer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
