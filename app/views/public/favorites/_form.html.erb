<%= form_with(model: public_favorite) do |form| %>
  <% if public_favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(public_favorite.errors.count, "error") %> prohibited this public_favorite from being saved:</h2>

      <ul>
        <% public_favorite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="check_box">
    <span>タグ</span>
    <%= form.collection_check_boxes(:tag_ids, Tag.all, :id, :name) do |tag| %>
     <div>
       <%= tag.label do %>
         <%= tag.check_box %>
         <%= tag.text %>
       <% end %>
     </div>
   <% end %>
 </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
